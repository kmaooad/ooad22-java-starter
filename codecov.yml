coverage:
  status:
    patch:
      default:
        # basic
        target: 80%
        threshold: 0%
        flags: 
          - unit
        paths: 
          - "!kmaooad-functions/src/test/"
      app:  # declare a new status context "app"
        paths:
          - "!kmaooad-functions/src/test/"  # remove all files in "tests/"
    project:
      default:
        # basic
        target: 80%
        threshold: 0%
        flags: 
          - unit
        paths: 
          - "!kmaooad-functions/src/test/"
      app:  # declare a new status context "app"
        paths:
          - "!kmaooad-functions/src/test/"  # remove all files in "tests/"